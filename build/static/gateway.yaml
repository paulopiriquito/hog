kind: Gateway
metadata: { name: hog }
spec:
  listen: ":8080"
---
kind: Route
metadata: { name: spa }
spec:
  match: /
  handler:
    type: static
    dir: /srv/web          # index.html default; spaFallback default true
  policy: { auth: public }
