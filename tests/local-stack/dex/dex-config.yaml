issuer: http://dex:5556

storage:
  type: sqlite3
  config:
    file: /var/dex/dex.db

web:
  http: 0.0.0.0:5556

oauth2:
  skipApprovalScreen: true

staticClients:
  - id: krakend
    redirectURIs:
      - 'http://localhost:3000/oauth/callback'
      - 'http://localhost:3000/oauth/pkce-callback'
    name: 'KrakenD Gateway'
    secret: krakend-secret
    skipApprovalScreen: true

enablePasswordDB: true

staticPasswords:
  - email: "test@example.com"
    hash: "$2a$10$2b2cU8CPhOTaGrs1HRQuAueS7JTT5ZHsHSzYiFPm1leZck7Mc8T4W"
    username: "test"
    userID: "08a8684b-db88-4b73-90a9-3cd1661f5466"
    groups:
      # Application-role groups (match the forward.headers mapping rules).
      - "cn=PT-XP-ROLE-APP-USER,ou=applicationRole,o=corp"
      - "cn=PT-XP-ROLE-APP-ADMIN,ou=applicationRole,o=corp"
      - "cn=GLOBAL-ROLE-GITHUB,ou=applicationRole,o=corp"
      # Noise group (no matching rule, must be filtered out).
      - "cn=PT-XP-HEADOFFICE,ou=people,o=corp"
