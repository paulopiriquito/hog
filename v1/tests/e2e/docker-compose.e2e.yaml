name: hog-e2e

services:
  e2e-web:
    build:
      context: ../e2e/web
    networks:
      - hog-local-stack_hog-network

  e2e-api:
    build:
      context: ../e2e/api
    networks:
      - hog-local-stack_hog-network

networks:
  hog-local-stack_hog-network:
    external: true
